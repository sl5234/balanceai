# Status

<!-- INSTRUCTIONS: Clear everything below and replace with a fresh summary each session. Keep it short — 3-4 sentences max. -->

## Session: 2026-08-17

Backend is now self-contained under src/balanceai_backend/ (pyproject.toml, requirements.txt, tests/, integ_tests/, DEVELOPMENT.md moved in from root; CLAUDE.md/AGENTS.md updated) — committed. Packaging needed an explicit `packages`/`package-dir` list in pyproject.toml instead of `[tool.setuptools.packages.find]`, which silently breaks in this monorepo layout (namespace-package discovery sweeps in sibling dirs like balanceai_frontend/balanceai_infrastructure, or a `where=".."`-style config gets a wheel that builds "successfully" but is empty) — if pyproject.toml packaging config is touched again, read that list before changing it. New src/balanceai_infrastructure/ CDK TypeScript app scaffolded (depends on appdevcommons-cdk's ApiGatewayToLambda construct), account 792341830430/us-west-2 bootstrapped, empty stack deployed successfully as a pipeline check — committed. Docker (via Colima) is installed and running locally for Lambda dependency bundling. Next: the actual Lambda deployment path — write requirements-lambda.txt (backend deps minus google-cloud-aiplatform) and a stub Python handler, a Docker pre-build script to vendor deps into a clean directory (matching Colima's arm64 daemon), wire ApiGatewayToLambda into the stack pointed at that directory, deploy, then verify via `aws lambda get-function` + unzip that the deployed package actually contains the right dependencies (doesn't need to run yet, just needs the right packages inside). Also found but did not fix: `mcp` is unpinned in pyproject.toml and today's PyPI version doesn't have `mcp.server.fastmcp` anymore, breaking `tests/servers/test_bookkeeping_server.py` and `test_report_definition.py` — pre-existing, unrelated to this session's work.