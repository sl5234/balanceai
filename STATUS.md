# Status

<!-- INSTRUCTIONS: Clear everything below and replace with a fresh summary each session. Keep it short — 3-4 sentences max. -->

## Session: 2026-08-20

Built the Plaid bank-connection flow through the Hosted Link handshake, live-verified against a real Plaid Sandbox connection (Tartan Bank, 14 test accounts) — committed (`4edc1d6`). New `services/plaid.py` (lazy `lru_cache`d client factory) and `bank_link/` package: `plaid_item_db.py`/`plaid_sync_cursor_db.py` split into two tables (static bank-connection identity vs. high-churn sync-progress state, so every sync call doesn't have to touch the row holding `access_token`), and `link.py` (`python -m balanceai_backend.bank_link.link`) for the one-time CLI setup. The live test caught two real bugs no static check would have: Plaid's generated SDK models raise `ApiAttributeError` on unset optional fields instead of returning `None`, and the actual Hosted Link success signal is `link_sessions[i].results.item_add_results[0]` — not the `on_success` field the SDK's model schema implies but a real response never populates. Plaid creds are now plain env vars via `.env` (gitignored, plus a global Claude Code permission rule blocking Claude from reading `.env` at all), replacing a stale unused KMS-encrypted setting. Also merged in another agent's `db.py` → `db/` package split from earlier the same session. Next: `bank_link/sync.py` (the `/transactions/sync` loop) and wiring `servers/link_bank_server.py`'s tools onto the new storage — two open decisions before writing it, noted in `docs/PLAID_INTEGRATION_DECISIONS.md`: whether to key local transaction storage by Plaid's raw `account_id` (the Plaid-account↔our-account mapping is deliberately deferred), and adding a `delete_transaction` function to `statements/storage.py` (doesn't exist yet) to handle Plaid's `removed` bucket.